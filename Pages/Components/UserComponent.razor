
<span class="form-control form-control-sm bg-secondary text-white fs-5 mt-2 mb-2" readonly>
    @title: @currentUser
</span>

@code {
    [Parameter]
    public string title { get; set; } = string.Empty;

    [Parameter]
    public string currentUser { get; set; } = string.Empty;
}
